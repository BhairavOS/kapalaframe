#pragma once

#include <kapalaframe-base/string.h>

namespace Kapalaframe {

Str witty(usize seed);

Str nice(usize seed);

Str wholesome(usize seed);

Str goodEmoji(usize seed);

Str badEmoji(usize seed);

} // namespace Kapalaframe
